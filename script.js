var tc = prompt('Введите температуру в градусах по Цельсию', '');
var tf = 1.8 * tc + 32;
alert ('Температура в градусах по Фаренгейту равна ' + tf);

var admin, name;
var name = prompt('Введите значение переменной name ', '');
var admin = name;
alert ('Значение переменной admin ' + admin);
